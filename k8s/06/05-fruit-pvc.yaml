apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: fruit-pvc
  namespace: name1
  labels:
    app: fruit-api
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: do-block-storage